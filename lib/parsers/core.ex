defmodule Mu.Parser.Core do
  @moduledoc """
  This parser module contains core functionality that is applicable to all applications 

  """
  def parse(command, tail) do
  end

  def as_is(command, tail) do
  end
end
